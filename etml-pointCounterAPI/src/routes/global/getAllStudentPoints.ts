import express, {Request, Response, Express} from "express";

const getAllStudentPointsPerModuleRouter = express.Router();

getAllStudentPointsPerModuleRouter.get("/", (req: Request, res: Response) => {
    
})